{
  "name": "frameflow-storyboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test",
    "dev": "npm run build && node --env-file-if-exists=.env scripts/dev.mjs"
  },
  "type": "module"
}
